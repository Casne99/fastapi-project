{
  "name": "frontend-tests",
  "version": "1.0.0",
  "description": "Test Playwright per la frontend",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:docker": "xvfb-run -a --server-args=\"-screen 0 1920x1080x24\" playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
